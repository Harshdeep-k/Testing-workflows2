name: Secondary Workflow

on:
  workflow_call:
  

jobs:
  run-if-triggered:
    runs-on: ubuntu-latest
    steps:
      - name: Check Trigger
        run: |
            echo "The workflow from another repo"
  
